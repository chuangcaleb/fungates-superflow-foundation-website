import type { Block } from 'payload'

export const ContactBlock: Block = {
  slug: 'contact',
  interfaceName: 'ContactBlock',
  fields: [],
  labels: {
    plural: 'Contacts',
    singular: 'Contact',
  },
}
